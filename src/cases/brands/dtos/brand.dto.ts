export interface BrandDTO{ //pode ser usada por outra classe
  id?: string;
  name: string;
}
//não será necessário a obrigatoriedade do id na criação de uma categoria, a não obrigatoriedade de um atributo
//pode ser feito adicionando um '?' ao lado do campo como no id?

